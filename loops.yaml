- name: loops exmaple
  hosts: web
  become: yes
  vars:
    package_list:
    - mysql
    - nginx
    - postfix
  tasks:
  - name: installing packages
    ansible.builtin.package:
    name: "{{ item }}" 
    state: present


